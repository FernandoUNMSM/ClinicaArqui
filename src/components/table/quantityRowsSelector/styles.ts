import styled from 'styled-components'

export const SelectQuantity = styled.div`
  height: 40px;
  .buttonSelect {
    /* box-shadow: ${(props: any) => props.theme.bs.optionModal}; */
    background-color: ${(props) => props.theme.colors.background.dashboard}!important;
  }
`
